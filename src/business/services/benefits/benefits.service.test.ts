import chai from 'chai';
const assert = chai.assert;

describe('Adder', () => {

    describe('Add numbers', () => {
        it('Can add.', () => {
            const sum = 3 + 2;

            assert.equal(sum, 5);

        });
    });
    
});